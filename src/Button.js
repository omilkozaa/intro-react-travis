import React from 'react';

/*const Button = ({onClick,text}) => (
    <button style={{height: '10px',minWidth: '20px', paddingRight:'5px'}} onClick={props.onclick}>{props.text}</button>
);*/
export default Button;